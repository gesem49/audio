'use strict';

const GET = require('../controllers/static-get');

const Static = {
  alias: 'Static',
  sign: /^\/static(\/(cascades|scripts|fonts|views)([\/a-zA-Z0-9\-\_\.]*))$/,
  controllers: {
    GET
  },
  query: {
    allowed_methods: ['GET']
  }
}

module.exports = Static;
