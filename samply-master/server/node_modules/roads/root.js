'use strict';

const GET = require('../controllers/root-get');

const Root = {
  alias: 'Root',
  sign: /^\/$/,
  controllers: {
    GET
  },
  query: {
    allowed_methods: ['GET']
  }
}

module.exports = Root;
